How to install:
	- Copy the contents of this directory into a permanent directory with no spaces in the path
	- Open "install.cmd" with a text editor and replace the directory in the directory variable
	- Open install.cmd as administrator