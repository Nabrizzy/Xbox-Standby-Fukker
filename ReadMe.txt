How to install:
- Compile the project and copy the content into a permanent directory with no spaces in the path (e.g "C:\Service\XboxStandbyFukker")
- Open "install.cmd" with a text editor and replace the directory in the directory variable
- Open install.cmd as administrator